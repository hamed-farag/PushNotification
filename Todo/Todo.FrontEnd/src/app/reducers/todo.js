import {
    FETCH_TODO
} from 'constants/todo';

const INITIAL_STATE = {
    todo: {}
};

export default function (state = INITIAL_STATE, action) {
    switch (action.type) {
        default: return state;
    }
}